schema_version: "1.0"
skill_id: "flowstate-adapter-capability-check"
display_name: "Flowstate Adapter Capability Check"
description: "Apply the FlowState platform adapters and session transport workflow when the concrete scenario is: Check whether a platform supports a required operation. Produce capability result with evidence and status."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["Check whether a platform supports a required operation"]
positive_triggers: ["Check whether a platform supports a required operation"]
negative_triggers: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
boundaries: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
required_inputs: ["classified project context","plan or task-local artifacts","current constraints"]
expected_outputs: ["capability result","evidence, risks, blockers, and next action"]
prerequisites: ["FlowState startup classification","declared scope"]
side_effects: ["may create governed planning, execution, review, or audit records"]
risk_level: "high"
priority: 50
category: "adapters"
subcategory: "adapter-capability-check"
tags: ["adapters","adapter-capability-check"]
