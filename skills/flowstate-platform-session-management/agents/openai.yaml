interface: {"display_name":"Flowstate Platform Session Management","short_description":"Adapters workflow for session mapping","default_prompt":"Use $flowstate-platform-session-management for Create and reuse real controller and worker sessions and produce session mapping."}
policy: {"allow_implicit_invocation":true}
