schema_version: "1.0"
skill_id: "flowstate-platform-session-management"
display_name: "Flowstate Platform Session Management"
description: "Apply the FlowState platform adapters and session transport workflow when the concrete scenario is: Create and reuse real controller and worker sessions. Produce session mapping with evidence and status."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["Create and reuse real controller and worker sessions"]
positive_triggers: ["Create and reuse real controller and worker sessions"]
negative_triggers: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
boundaries: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
required_inputs: ["classified project context","plan or task-local artifacts","current constraints"]
expected_outputs: ["session mapping","evidence, risks, blockers, and next action"]
prerequisites: ["FlowState startup classification","declared scope"]
side_effects: ["may create governed planning, execution, review, or audit records"]
risk_level: "high"
priority: 50
category: "adapters"
subcategory: "platform-session-management"
tags: ["adapters","platform-session-management"]
