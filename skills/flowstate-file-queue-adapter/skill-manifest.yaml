schema_version: "1.0"
skill_id: "flowstate-file-queue-adapter"
display_name: "Flowstate File Queue Adapter"
description: "Apply the FlowState platform adapters and session transport workflow when the concrete scenario is: Use an auditable local queue without a conversation API. Produce queue message with evidence and status."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["Use an auditable local queue without a conversation API"]
positive_triggers: ["Use an auditable local queue without a conversation API"]
negative_triggers: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
boundaries: ["Do not use outside platform adapters and session transport scenarios","Do not bypass approval, scope, evidence, or safety gates"]
required_inputs: ["classified project context","plan or task-local artifacts","current constraints"]
expected_outputs: ["queue message","evidence, risks, blockers, and next action"]
prerequisites: ["FlowState startup classification","declared scope"]
side_effects: ["may create governed planning, execution, review, or audit records"]
risk_level: "high"
priority: 50
category: "adapters"
subcategory: "file-queue-adapter"
tags: ["adapters","file-queue-adapter"]
