interface:
  display_name: "PDGO Dialogue Memory"
  short_description: "Preserve indexed conversation memory and handoff context"
  default_prompt: "Use $pdgo-dialogue-memory for conversation memory, checkpoint, retrieval, and handoff and return governed evidence."
policy:
  allow_implicit_invocation: true
