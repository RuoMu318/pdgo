schema_version: "1.0"
skill_id: "flowstate-retry-escalation"
display_name: "Flowstate Retry Escalation"
description: "Apply the FlowState controllers, dispatch, and agents workflow when the concrete scenario is: Retry bounded failures and escalate after the limit. Produce retry decision with evidence and status."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["Retry bounded failures and escalate after the limit"]
positive_triggers: ["Retry bounded failures and escalate after the limit"]
negative_triggers: ["Do not use outside controllers, dispatch, and agents scenarios","Do not bypass approval, scope, evidence, or safety gates"]
boundaries: ["Do not use outside controllers, dispatch, and agents scenarios","Do not bypass approval, scope, evidence, or safety gates"]
required_inputs: ["classified project context","plan or task-local artifacts","current constraints"]
expected_outputs: ["retry decision","evidence, risks, blockers, and next action"]
prerequisites: ["FlowState startup classification","declared scope"]
side_effects: ["may create governed planning, execution, review, or audit records"]
risk_level: "high"
priority: 50
category: "dispatch"
subcategory: "retry-escalation"
tags: ["dispatch","retry-escalation"]
