schema_version: "1.0"
skill_id: "flowstate-writing-plans"
display_name: "FlowState Writing Plans"
description: "Produce a plan whose title, summary, search terms, knowledge domains, tasks, risks, blockers, and approval request are explicit."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["a plan needs a clear human-readable artifact"]
positive_triggers: ["A new plan or plan version is requested","Other conversations need to retrieve the plan"]
negative_triggers: ["Do not write an execution command as a plan","Do not omit an empty-section explanation"]
boundaries: ["Do not write an execution command as a plan","Do not omit an empty-section explanation"]
required_inputs: ["goal","context","constraints","decisions"]
expected_outputs: ["plan document","plan index entry","approval request"]
prerequisites: ["scenario classification"]
side_effects: ["writes planning records"]
risk_level: "low"
priority: 50
category: "planning"
subcategory: "writing-plans"
tags: ["planning","writing-plans"]
