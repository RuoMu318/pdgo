schema_version: "1.0"
skill_id: "flowstate-tdd"
display_name: "FlowState Test Driven Development"
description: "Use a test-first loop inside the approved task scope and return the test evidence with the change."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["a bounded code change needs test-first implementation"]
positive_triggers: ["The task has a deterministic behavior contract","The project uses automated tests"]
negative_triggers: ["Do not invent acceptance criteria","Do not skip the verification loop"]
boundaries: ["Do not invent acceptance criteria","Do not skip the verification loop"]
required_inputs: ["behavior contract","test harness","approved paths"]
expected_outputs: ["tests","implementation","test log","deviations"]
prerequisites: ["test runner","approved dispatch"]
side_effects: ["writes tests and implementation files"]
risk_level: "medium"
priority: 50
category: "review"
subcategory: "test-driven-development"
tags: ["review","test-driven-development"]
