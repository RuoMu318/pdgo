schema_version: "1.0"
skill_id: "flowstate-git-worktree"
display_name: "FlowState Git Worktree"
description: "Create bounded worktrees for concurrent writers and record paths, branches, cleanup, and merge evidence."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["concurrent task workers need isolated Git workspaces"]
positive_triggers: ["Independent tasks write to the same repository","A parallel dispatch needs workspace isolation"]
negative_triggers: ["Do not rewrite another worker's worktree","Do not delete worktrees without approval"]
boundaries: ["Do not rewrite another worker's worktree","Do not delete worktrees without approval"]
required_inputs: ["repository","branch policy","task IDs"]
expected_outputs: ["worktree mapping","cleanup record","merge evidence"]
prerequisites: ["Git repository","parallel isolation decision"]
side_effects: ["creates Git worktrees"]
risk_level: "medium"
priority: 50
category: "execution"
subcategory: "git-worktree"
tags: ["execution","git-worktree"]
