schema_version: "1.0"
skill_id: "flowstate-memory-retrieval"
display_name: "FlowState Memory Retrieval"
description: "Find conversation and plan records through indexes first, read summaries before source turns, and record every reference."
project_scope: ["flowstate"]
department: ["planning","execution","review"]
stage: ["discovery","design","implementation","validation"]
scenarios: ["a task needs information from another conversation"]
positive_triggers: ["The current task depends on another conversation","A plan or knowledge record must be found"]
negative_triggers: ["Do not assume a transcript was shared","Do not use draft memory as an execution fact"]
boundaries: ["Do not assume a transcript was shared","Do not use draft memory as an execution fact"]
required_inputs: ["project ID","search terms","knowledge domain"]
expected_outputs: ["retrieved summaries","source references","context decision"]
prerequisites: ["project memory indexes"]
side_effects: ["records cross-session references"]
risk_level: "medium"
priority: 50
category: "memory"
subcategory: "memory-retrieval"
tags: ["memory","memory-retrieval"]
