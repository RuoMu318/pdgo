# Tests

Tests validate the repository contracts, Skill metadata, plugin manifests, and compatibility guarantees. Runtime
integration tests belong beside the adapter they exercise and must provide reproducible evidence.
