# Tests

Tests validate the repository contracts, Skill metadata, plugin manifests, and compatibility guarantees. Node tests
cover Skill generation, series session continuity, approval, dispatch, report/review handoff, blocker waiting, plan
indexing, parallel safety, and the file queue. Runtime integration tests belong beside the adapter they exercise and
must provide reproducible evidence.
