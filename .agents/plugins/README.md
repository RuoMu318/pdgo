# Plugin registry

This directory is reserved for repository-local plugin catalog entries and release metadata.
The canonical PDGO plugin manifest is at `../..\.codex-plugin\plugin.json`.

Keep registry entries additive and versioned. Do not store credentials, local paths, generated runtime state,
or mutable session data here.
