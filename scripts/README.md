# Scripts

Scripts in this directory must be deterministic, safe to rerun, and narrowly scoped. They may validate contracts,
rebuild derived indexes, or adapt records between supported formats. Scripts must not store credentials or silently
modify product repositories outside the explicit task scope.
